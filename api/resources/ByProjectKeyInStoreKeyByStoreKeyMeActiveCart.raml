#ByProjectKeyInStoreKeyByStoreKeyMeActiveCart
(resourcePathUri): /{projectKey}/in-store/key={storeKey}/me/active-cart

uriParameters:
  projectKey:
    type: string
  storeKey:
    type: string
get:
  responses:
    200:
      body:
        application/json:
          type: Cart